#cloud-config
autoinstall:
  version: 1
  identity:
    username: <username>
    password: <password>
  locale: en_US
  keyboard:
    layout: en
  ssh:
    install-server: true
    allow-pw: true
    # disable_root: true
    # ssh_quiet_keygen: true
    # allow_public_ssh_keys: true
  packages:
    - qemu-guest-agent
    - sudo
  storage:
    layout:
      name: direct
    swap:
      size: 0
  user-data:
    package_upgrade: false
    timezone: Europe/Berlin
    users:
      - name: <username>
        groups: [adm, sudo]
        lock-passwd: false
        sudo: ALL=(ALL) NOPASSWD:ALL
        shell: /bin/bash
        passwd: <password>
        # - or -
        # ssh_authorized_keys:
        #   - "~/.ssh/id_ed25519_shellbender_underhill"