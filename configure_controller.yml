---
- name: Configure tier 0 controller
  hosts: localhost
  gather_facts: false
  vars:
    packages:
      - sshpass
      - rpi-imager
  tasks:
    - name: Install controller packages
      ansible.builtin.package:
        name: "{{ packages }}"
        state: present
      become: true
